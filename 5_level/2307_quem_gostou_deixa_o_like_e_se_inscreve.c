#include <stdio.h>

bool solved()
{
    int x, y, n;
    cin >> x >> y >> n;

    int array[n];
    
    for (int i = 0; i < n; i++)
        cin >> array[i];

    for (int i = 0; i < count; i++)
    {
        /* code */
    }
    
}

int main ()
{
    
    

    return 0;
}